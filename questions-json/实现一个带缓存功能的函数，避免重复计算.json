{
  "audioKey": "q793d1b4e",
  "id": "d8e7f6g5-h4i3-2109-jklm-nopqrstuvwxy",
  "type": "answer",
  "difficulty": "medium",
  "tags": [
    "javascript"
  ],
  "question_markdown": "实现一个带缓存功能的函数，避免重复计算",
  "answer_simple_markdown": "・用一个对象作为缓存容器，存储函数参数与计算结果的映射\n\n・调用函数时先检查缓存，存在对应参数的结果则直接返回，避免重复计算\n\n・首次调用或参数未命中缓存时，执行原函数计算，将结果存入缓存后返回\n\n・需处理参数序列化，确保不同形式的相同参数能命中同一缓存（如数组、对象参数）\n\n・可扩展支持缓存过期、缓存大小限制等功能",
  "answer_detail_markdown": "带缓存的函数主要用来优化那些计算量大或者调用频繁的函数，比如复杂的数学计算、重复请求相同数据的操作。核心思路就是把之前算过的结果存起来，下次用一样的参数调用时，直接拿存好的结果，不用再重新计算。\n\n实现的时候，通常会用一个内部对象当缓存库，键是函数的参数，值是对应的计算结果。调用函数前先看看参数在缓存里有没有记录，有的话直接返回值；没有的话就正常执行函数，算完之后把参数和结果存到缓存里，方便下次用。\n\n不过这里有个关键点，就是参数怎么当键。如果参数是简单的数字、字符串还好，直接用就行。但如果是对象或者数组，就不能直接当键了，得转成字符串，比如用 JSON.stringify ()，这样才能保证相同内容的对象参数能命中同一个缓存。另外，还可以根据需要加一些功能，比如限制缓存的大小，防止存太多东西占内存，或者给缓存设置过期时间，避免数据太旧。\n\n这种带缓存的函数在实际开发中很有用，比如处理用户频繁切换的筛选条件计算，或者缓存接口返回的相同数据，能明显提高性能，减少等待时间。",
  "answer_analysis_markdown": "### 实现原理\n\n带缓存功能的函数（也称为 \"记忆化函数\"）基于**空间换时间**的优化思想，通过存储函数的输入参数与输出结果的映射关系，避免对相同参数进行重复计算。其核心机制包括：\n\n\n\n1.  缓存容器：用于存储参数与结果的映射（通常为对象或 Map）\n\n2.  参数序列化：将输入参数转换为可作为缓存键的字符串\n\n3.  缓存命中判断：调用时先检查参数是否已存在于缓存中\n\n4.  缓存更新策略：首次计算后将结果存入缓存\n\n### 基础版实现\n\n\n\n```\n/\\*\\*\n\n&#x20;\\* 创建带缓存功能的函数\n\n&#x20;\\* @param {Function} fn - 需要添加缓存的原函数\n\n&#x20;\\* @returns {Function} 带缓存的函数\n\n&#x20;\\*/\n\nfunction withCache(fn) {\n\n&#x20; // 用对象作为缓存容器，键为序列化后的参数，值为函数计算结果\n\n&#x20; const cache = {};\n\n&#x20;&#x20;\n\n&#x20; // 返回一个新函数，该函数会先检查缓存\n\n&#x20; return function(...args) {\n\n&#x20;   // 1. 参数序列化：将参数数组转换为字符串作为缓存键\n\n&#x20;   // 使用JSON.stringify可以处理数组、对象等复杂参数\n\n&#x20;   const key = JSON.stringify(args);\n\n&#x20;  &#x20;\n\n&#x20;   // 2. 检查缓存：如果命中则直接返回缓存结果\n\n&#x20;   if (cache.hasOwnProperty(key)) {\n\n&#x20;     console.log(\\`缓存命中：参数 \\${key}\\`);\n\n&#x20;     return cache\\[key];\n\n&#x20;   }\n\n&#x20;  &#x20;\n\n&#x20;   // 3. 未命中缓存：执行原函数计算结果\n\n&#x20;   console.log(\\`缓存未命中：参数 \\${key}，执行计算\\`);\n\n&#x20;   const result = fn.apply(this, args); // 保持原函数的this上下文\n\n&#x20;  &#x20;\n\n&#x20;   // 4. 更新缓存：将计算结果存入缓存\n\n&#x20;   cache\\[key] = result;\n\n&#x20;  &#x20;\n\n&#x20;   // 5. 返回计算结果\n\n&#x20;   return result;\n\n&#x20; };\n\n}\n```\n\n#### 基础版使用示例\n\n\n\n```\n// 示例1：计算斐波那契数列（计算密集型函数）\n\nfunction fibonacci(n) {\n\n&#x20; if (n <= 1) return n;\n\n&#x20; return fibonacci(n - 1) + fibonacci(n - 2);\n\n}\n\n// 为斐波那契函数添加缓存\n\nconst cachedFibonacci = withCache(fibonacci);\n\n// 首次调用（未命中缓存）\n\nconsole.log(cachedFibonacci(10)); // 输出55，会执行完整计算\n\n// 重复调用（命中缓存）\n\nconsole.log(cachedFibonacci(10)); // 输出55，直接返回缓存结果\n\n// 示例2：处理对象参数\n\nfunction getUserInfo(user) {\n\n&#x20; console.log('执行用户信息计算');\n\n&#x20; return {\n\n&#x20;   id: user.id,\n\n&#x20;   name: user.name,\n\n&#x20;   fullInfo: \\`ID: \\${user.id}, Name: \\${user.name}\\`\n\n&#x20; };\n\n}\n\nconst cachedGetUserInfo = withCache(getUserInfo);\n\nconst user = { id: 1, name: 'Alice' };\n\ncachedGetUserInfo(user); // 未命中缓存，执行计算\n\ncachedGetUserInfo(user); // 命中缓存，直接返回结果\n```\n\n### 进阶版实现（带缓存管理功能）\n\n基础版仅实现了核心缓存功能，实际应用中还需要考虑缓存过期、缓存大小限制等问题，以下是增强版实现：\n\n\n\n```\n/\\*\\*\n\n&#x20;\\* 创建带高级缓存功能的函数\n\n&#x20;\\* @param {Function} fn - 需要添加缓存的原函数\n\n&#x20;\\* @param {Object} options - 缓存配置\n\n&#x20;\\* @param {number} \\[options.maxSize=Infinity] - 最大缓存数量\n\n&#x20;\\* @param {number} \\[options.maxAge=Infinity] - 缓存过期时间（毫秒）\n\n&#x20;\\* @param {Function} \\[options.serializer=JSON.stringify] - 参数序列化函数\n\n&#x20;\\* @returns {Function} 带高级缓存的函数\n\n&#x20;\\*/\n\nfunction advancedWithCache(fn, options = {}) {\n\n&#x20; // 解析配置参数，设置默认值\n\n&#x20; const {\n\n&#x20;   maxSize = Infinity,\n\n&#x20;   maxAge = Infinity,\n\n&#x20;   serializer = JSON.stringify\n\n&#x20; } = options;\n\n&#x20;&#x20;\n\n&#x20; // 缓存容器：存储结构为 { 键: { value: 结果, timestamp: 时间戳 } }\n\n&#x20; const cache = new Map();\n\n&#x20;&#x20;\n\n&#x20; // 清理过期缓存的方法\n\n&#x20; function cleanExpiredCache() {\n\n&#x20;   const now = Date.now();\n\n&#x20;   // 遍历缓存，删除过期条目\n\n&#x20;   for (const \\[key, entry] of cache.entries()) {\n\n&#x20;     if (now - entry.timestamp > maxAge) {\n\n&#x20;       cache.delete(key);\n\n&#x20;       console.log(\\`缓存过期：\\${key}\\`);\n\n&#x20;     }\n\n&#x20;   }\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; // 清理超出容量的缓存（使用LRU策略：最近最少使用）\n\n&#x20; function cleanExcessCache() {\n\n&#x20;   if (cache.size <= maxSize) return;\n\n&#x20;  &#x20;\n\n&#x20;   // 获取缓存条目并按访问时间排序（最早访问的在前）\n\n&#x20;   const sortedEntries = Array.from(cache.entries()).sort(\n\n&#x20;     (\\[, a], \\[, b]) => a.timestamp - b.timestamp\n\n&#x20;   );\n\n&#x20;  &#x20;\n\n&#x20;   // 计算需要删除的数量\n\n&#x20;   const needToDelete = cache.size - maxSize;\n\n&#x20;  &#x20;\n\n&#x20;   // 删除最早访问的条目\n\n&#x20;   for (let i = 0; i < needToDelete; i++) {\n\n&#x20;     const \\[key] = sortedEntries\\[i];\n\n&#x20;     cache.delete(key);\n\n&#x20;     console.log(\\`缓存溢出，删除最早条目：\\${key}\\`);\n\n&#x20;   }\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; // 缓存函数本体\n\n&#x20; const cachedFn = function(...args) {\n\n&#x20;   // 1. 清理过期缓存\n\n&#x20;   cleanExpiredCache();\n\n&#x20;  &#x20;\n\n&#x20;   // 2. 序列化参数生成缓存键\n\n&#x20;   const key = serializer(args);\n\n&#x20;  &#x20;\n\n&#x20;   // 3. 检查缓存是否命中\n\n&#x20;   if (cache.has(key)) {\n\n&#x20;     const entry = cache.get(key);\n\n&#x20;     // 更新访问时间（用于LRU策略）\n\n&#x20;     entry.timestamp = Date.now();\n\n&#x20;     console.log(\\`缓存命中：\\${key}\\`);\n\n&#x20;     return entry.value;\n\n&#x20;   }\n\n&#x20;  &#x20;\n\n&#x20;   // 4. 未命中缓存，执行原函数计算\n\n&#x20;   console.log(\\`缓存未命中：\\${key}，执行计算\\`);\n\n&#x20;   const result = fn.apply(this, args);\n\n&#x20;  &#x20;\n\n&#x20;   // 5. 清理超出容量的缓存\n\n&#x20;   cleanExcessCache();\n\n&#x20;  &#x20;\n\n&#x20;   // 6. 存入新缓存，记录当前时间戳\n\n&#x20;   cache.set(key, {\n\n&#x20;     value: result,\n\n&#x20;     timestamp: Date.now()\n\n&#x20;   });\n\n&#x20;  &#x20;\n\n&#x20;   // 7. 返回计算结果\n\n&#x20;   return result;\n\n&#x20; };\n\n&#x20;&#x20;\n\n&#x20; // 为缓存函数添加手动管理方法\n\n&#x20; cachedFn.clearCache = () => {\n\n&#x20;   cache.clear();\n\n&#x20;   console.log('缓存已清空');\n\n&#x20; };\n\n&#x20;&#x20;\n\n&#x20; cachedFn.getCacheSize = () => cache.size;\n\n&#x20;&#x20;\n\n&#x20; return cachedFn;\n\n}\n```\n\n#### 进阶版使用示例\n\n\n\n```\n// 模拟一个耗时的API请求函数\n\nfunction fetchData(url, params) {\n\n&#x20; console.log(\\`实际请求：\\${url}\\`);\n\n&#x20; // 模拟网络延迟\n\n&#x20; return new Promise(resolve => {\n\n&#x20;   setTimeout(() => {\n\n&#x20;     resolve({\n\n&#x20;       url,\n\n&#x20;       params,\n\n&#x20;       data: \\`模拟数据：\\${Math.random()}\\`\n\n&#x20;     });\n\n&#x20;   }, 1000);\n\n&#x20; });\n\n}\n\n// 创建带缓存的请求函数：缓存有效期5秒，最多缓存10条\n\nconst cachedFetch = advancedWithCache(fetchData, {\n\n&#x20; maxSize: 10,\n\n&#x20; maxAge: 5000 // 5秒过期\n\n});\n\n// 使用场景：短时间内多次请求相同数据\n\nasync function test() {\n\n&#x20; // 首次请求（未命中缓存）\n\n&#x20; const result1 = await cachedFetch('https://api.example.com/data', { id: 1 });\n\n&#x20; console.log('结果1:', result1.data);\n\n&#x20;&#x20;\n\n&#x20; // 1秒后再次请求（命中缓存）\n\n&#x20; setTimeout(async () => {\n\n&#x20;   const result2 = await cachedFetch('https://api.example.com/data', { id: 1 });\n\n&#x20;   console.log('结果2:', result2.data); // 与结果1相同（来自缓存）\n\n&#x20; }, 1000);\n\n&#x20;&#x20;\n\n&#x20; // 6秒后请求（缓存已过期）\n\n&#x20; setTimeout(async () => {\n\n&#x20;   const result3 = await cachedFetch('https://api.example.com/data', { id: 1 });\n\n&#x20;   console.log('结果3:', result3.data); // 新的结果（重新请求）\n\n&#x20; }, 6000);\n\n}\n\ntest();\n```\n\n### 关键技术点解析\n\n\n\n1.  **参数序列化策略**\n\n\n\n```\n// 自定义序列化函数示例（忽略函数参数）\n\nfunction customSerializer(args) {\n\n&#x20; return JSON.stringify(args.map(arg =>&#x20;\n\n&#x20;   typeof arg === 'function' ? '\\[Function]' : arg\n\n&#x20; ));\n\n}\n```\n\n\n\n*   简单类型（字符串、数字）：可直接作为键\n\n*   复杂类型（对象、数组）：需序列化为字符串（如 JSON.stringify）\n\n*   特殊场景：可自定义序列化函数处理函数参数或循环引用对象\n\n1.  **缓存淘汰策略**\n\n*   LRU（Least Recently Used）：淘汰最近最少使用的缓存（进阶版采用）\n\n*   FIFO（First In First Out）：淘汰最早进入的缓存\n\n*   LFU（Least Frequently Used）：淘汰使用频率最低的缓存\n\n*   过期时间：基于时间的淘汰策略\n\n1.  **缓存容器选择**\n\n*   Object：简单场景使用，键只能是字符串\n\n*   Map：更适合缓存场景，支持任意类型作为键，且有内置迭代方法\n\n*   WeakMap：适合键为对象且需要自动回收的场景（当对象被销毁时自动删除缓存）\n\n1.  **与原函数的兼容性**\n\n*   使用`apply`保持原函数的`this`上下文\n\n*   通过剩余参数`...args`支持任意数量的参数\n\n*   对异步函数（返回 Promise）同样适用\n\n### 适用场景与注意事项\n\n#### 适用场景\n\n\n\n*   计算密集型函数（如数学计算、数据转换）\n\n*   频繁调用且参数重复度高的函数\n\n*   网络请求（缓存接口返回结果）\n\n*   配置解析、模板编译等一次性计算多次使用的场景\n\n#### 注意事项\n\n\n\n*   避免对副作用函数使用缓存（如修改全局变量、操作 DOM 的函数）\n\n*   注意缓存键的唯一性，避免不同参数序列化后得到相同键\n\n*   缓存会占用内存，需根据实际情况设置合理的缓存大小和过期时间\n\n*   对于结果会随时间变化的函数（如获取实时数据），必须设置过期时间\n\n### 性能对比\n\n以斐波那契数列计算为例，对比普通函数与带缓存函数的性能：\n\n\n\n```\n// 性能测试函数\n\nfunction performanceTest(fn, ...args) {\n\n&#x20; const start = Date.now();\n\n&#x20; const result = fn(...args);\n\n&#x20; const end = Date.now();\n\n&#x20; console.log(\\`结果: \\${result}, 耗时: \\${end - start}ms\\`);\n\n&#x20; return result;\n\n}\n\n// 测试普通版本（无缓存）\n\nperformanceTest(fibonacci, 35); // 约耗时数百毫秒\n\n// 测试缓存版本\n\nconst cachedFib = withCache(fibonacci);\n\nperformanceTest(cachedFib, 35); // 首次调用耗时与普通版相近\n\nperformanceTest(cachedFib, 35); // 缓存命中，耗时接近0ms\n```\n\n测试结果表明，对于计算密集型函数，缓存能带来数量级的性能提升，尤其是在重复调用时。\n\n### 总结\n\n实现带缓存功能的函数核心在于建立参数与结果的映射关系，并通过合理的缓存策略管理这些映射。基础版实现简单，适用于参数简单、计算稳定的场景；进阶版通过添加缓存过期、容量限制等功能，更适合生产环境使用。\n\n在实际开发中，记忆化缓存是一种简单有效的性能优化手段，尤其适合处理重复计算或重复请求的场景。但需注意缓存的维护成本和内存占用，避免过度缓存导致的问题。",
  "files": {
    "audio_answer_simple": "q793d1b4e_audio_answer_simple.mp3",
    "audio_answer_detail": "q793d1b4e_audio_answer_detail.mp3",
    "audio_answer_analysis": "q793d1b4e_audio_answer_analysis.mp3",
    "audio_question": "q793d1b4e_audio_question.mp3"
  }
}