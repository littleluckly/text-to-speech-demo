{
  "audioKey": "q5b43bef3",
  "id": "f6a7b8c9-d0e1-2345-fghi-67890abcdef",
  "type": "answer",
  "difficulty": "medium",
  "tags": [
    "设计模式"
  ],
  "question_markdown": "说说你对代理模式的理解？应用场景？",
  "answer_simple_markdown": "・代理模式是通过一个代理对象控制对原对象的访问，起到中介作用\n\n・核心作用是在不改变原对象的前提下，增加额外功能（如权限控制、缓存、日志）\n\n・代理与原对象实现相同接口，客户端无需区分两者\n\n・常见类型：保护代理（权限控制）、虚拟代理（延迟加载）、缓存代理（结果缓存）\n\n・适用于图片懒加载、接口请求代理、权限验证等场景",
  "answer_detail_markdown": "代理模式就像是生活中的中介，比如租房时的中介，你通过中介找房东租房，中介在中间会做一些事情，比如筛选房源、核实信息，但最终还是和房东产生实际交易。在代码里，就是用一个代理对象来包裹真实对象，所有对真实对象的访问都要经过代理。\n\n这种模式的好处很明显，能在不改动原有代码的情况下，给功能增加额外的处理。比如加载一张很大的图片，直接加载可能影响页面性能，这时候可以用虚拟代理，先显示占位图，等图片加载完成再替换，这就是代理帮我们做了延迟加载的工作。\n\n实际开发中用得挺多的，比如接口请求时，代理可以帮我们加请求头、处理错误；或者做权限控制，代理先检查用户有没有权限，有了再调用真实接口。还有缓存代理，比如计算一些复杂结果后，代理会把结果存起来，下次再要同样的结果就直接返回，不用重新计算了。代理和原对象接口一样，用的时候直接换就行，不影响其他代码。",
  "answer_analysis_markdown": "### 代理模式的本质与核心结构\n\n代理模式（Proxy Pattern）属于结构型设计模式，其核心思想是**通过引入一个代理对象，间接访问目标对象**，从而在不修改目标对象的前提下，为其增加额外功能或控制访问。\n\n代理模式的经典结构包含三个部分：\n\n\n\n1.  **抽象主题（Subject）**：定义目标对象和代理对象的共同接口，确保代理可被当作目标对象使用\n\n2.  **目标对象（Real Subject）**：实际执行业务逻辑的对象，是代理的目标\n\n3.  **代理对象（Proxy）**：实现抽象主题接口，持有目标对象的引用，控制对目标对象的访问，并可添加额外操作\n\n这种结构遵循 \"开闭原则\"，可以在不修改目标对象和客户端代码的情况下，灵活扩展功能。\n\n### 代理模式的实现方式\n\n#### 1. 基础实现框架\n\n\n\n```\n// 1. 抽象主题 - 定义共同接口\n\nclass Subject {\n\n&#x20; request() {\n\n&#x20;   throw new Error(\"子类必须实现request方法\");\n\n&#x20; }\n\n}\n\n// 2. 目标对象 - 实际业务逻辑\n\nclass RealSubject extends Subject {\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 实际的请求处理\n\n&#x20;  \\* @returns {string} 处理结果\n\n&#x20;  \\*/\n\n&#x20; request() {\n\n&#x20;   // 模拟真实业务逻辑\n\n&#x20;   return \"真实对象处理了请求\";\n\n&#x20; }\n\n}\n\n// 3. 代理对象 - 控制访问并添加功能\n\nclass ProxySubject extends Subject {\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 初始化代理\n\n&#x20;  \\* @param {RealSubject} realSubject - 目标对象\n\n&#x20;  \\*/\n\n&#x20; constructor(realSubject) {\n\n&#x20;   super();\n\n&#x20;   this.realSubject = realSubject; // 持有目标对象引用\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 代理的请求方法\n\n&#x20;  \\* @returns {string} 处理结果\n\n&#x20;  \\*/\n\n&#x20; request() {\n\n&#x20;   // 前置处理\n\n&#x20;   this.beforeRequest();\n\n&#x20;  &#x20;\n\n&#x20;   // 调用目标对象的方法\n\n&#x20;   const result = this.realSubject.request();\n\n&#x20;  &#x20;\n\n&#x20;   // 后置处理\n\n&#x20;   this.afterRequest();\n\n&#x20;  &#x20;\n\n&#x20;   return result;\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 请求前的额外操作\n\n&#x20;  \\*/\n\n&#x20; beforeRequest() {\n\n&#x20;   console.log(\"代理：请求前的准备工作\");\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 请求后的额外操作\n\n&#x20;  \\*/\n\n&#x20; afterRequest() {\n\n&#x20;   console.log(\"代理：请求后的清理工作\");\n\n&#x20; }\n\n}\n\n// 使用示例\n\n// 创建目标对象\n\nconst realSubject = new RealSubject();\n\n// 创建代理对象，包裹目标对象\n\nconst proxy = new ProxySubject(realSubject);\n\n// 客户端通过代理访问，无需知道真实对象\n\nconsole.log(proxy.request());\n\n// 输出：\n\n// 代理：请求前的准备工作\n\n// 真实对象处理了请求\n\n// 代理：请求后的清理工作\n```\n\n#### 2. ES6 Proxy 实现（JavaScript 特色）\n\nJavaScript 的 Proxy 对象原生支持代理模式，提供了更灵活的实现方式：\n\n\n\n```\n// 目标对象\n\nconst target = {\n\n&#x20; name: \"目标对象\",\n\n&#x20; doSomething: function() {\n\n&#x20;   return \\`\\${this.name}执行了操作\\`;\n\n&#x20; }\n\n};\n\n// 代理处理器\n\nconst handler = {\n\n&#x20; // 拦截属性访问\n\n&#x20; get(target, prop, receiver) {\n\n&#x20;   console.log(\\`访问了属性：\\${prop}\\`);\n\n&#x20;   // 可以添加权限控制等逻辑\n\n&#x20;   if (prop === 'secret' && !isAuthenticated()) {\n\n&#x20;     throw new Error(\"没有权限访问该属性\");\n\n&#x20;   }\n\n&#x20;   return Reflect.get(target, prop, receiver);\n\n&#x20; },\n\n&#x20;&#x20;\n\n&#x20; // 拦截方法调用\n\n&#x20; apply(target, thisArg, argumentsList) {\n\n&#x20;   console.log(\\`调用了方法，参数：\\${argumentsList}\\`);\n\n&#x20;   // 前置处理\n\n&#x20;   const start = Date.now();\n\n&#x20;  &#x20;\n\n&#x20;   // 执行原始方法\n\n&#x20;   const result = Reflect.apply(target, thisArg, argumentsList);\n\n&#x20;  &#x20;\n\n&#x20;   // 后置处理\n\n&#x20;   const end = Date.now();\n\n&#x20;   console.log(\\`方法执行耗时：\\${end - start}ms\\`);\n\n&#x20;  &#x20;\n\n&#x20;   return result;\n\n&#x20; },\n\n&#x20;&#x20;\n\n&#x20; // 拦截属性设置\n\n&#x20; set(target, prop, value, receiver) {\n\n&#x20;   console.log(\\`设置属性\\${prop}为\\${value}\\`);\n\n&#x20;   // 可以添加数据验证等逻辑\n\n&#x20;   if (prop === 'age' && (value < 0 || value > 150)) {\n\n&#x20;     throw new Error(\"年龄必须在0-150之间\");\n\n&#x20;   }\n\n&#x20;   return Reflect.set(target, prop, value, receiver);\n\n&#x20; }\n\n};\n\n// 创建代理对象\n\nconst proxy = new Proxy(target, handler);\n\n// 使用代理\n\nconsole.log(proxy.name); // 访问属性会被拦截\n\nproxy.age = 25; // 设置属性会被拦截\n\nproxy.doSomething(); // 调用方法会被拦截\n```\n\n### 代理模式的常见类型与应用场景\n\n#### 1. 虚拟代理（Virtual Proxy）\n\n延迟创建开销大的对象，直到真正需要时才创建，常用于优化性能。\n\n**图片懒加载实现**：\n\n\n\n```\n// 图片加载目标对象\n\nclass RealImage {\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 初始化真实图片\n\n&#x20;  \\* @param {string} src - 图片地址\n\n&#x20;  \\*/\n\n&#x20; constructor(src) {\n\n&#x20;   this.src = src;\n\n&#x20;   this.loadImage(); // 加载图片（耗时操作）\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 显示图片\n\n&#x20;  \\* @param {HTMLElement} container - 容器元素\n\n&#x20;  \\*/\n\n&#x20; display(container) {\n\n&#x20;   const img = document.createElement('img');\n\n&#x20;   img.src = this.src;\n\n&#x20;   container.appendChild(img);\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 实际加载图片\n\n&#x20;  \\*/\n\n&#x20; loadImage() {\n\n&#x20;   console.log(\\`正在加载图片：\\${this.src}\\`);\n\n&#x20;   // 模拟网络请求延迟\n\n&#x20;   return new Promise(resolve => {\n\n&#x20;     setTimeout(() => {\n\n&#x20;       console.log(\\`图片加载完成：\\${this.src}\\`);\n\n&#x20;       resolve();\n\n&#x20;     }, 1000);\n\n&#x20;   });\n\n&#x20; }\n\n}\n\n// 图片代理（虚拟代理）\n\nclass ImageProxy {\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 初始化图片代理\n\n&#x20;  \\* @param {string} src - 图片地址\n\n&#x20;  \\*/\n\n&#x20; constructor(src) {\n\n&#x20;   this.src = src;\n\n&#x20;   this.realImage = null; // 延迟初始化真实图片\n\n&#x20;   this.placeholder = 'data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==';\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 显示图片（代理方法）\n\n&#x20;  \\* @param {HTMLElement} container - 容器元素\n\n&#x20;  \\*/\n\n&#x20; display(container) {\n\n&#x20;   // 先显示占位图\n\n&#x20;   const placeholderImg = document.createElement('img');\n\n&#x20;   placeholderImg.src = this.placeholder;\n\n&#x20;   placeholderImg.className = 'placeholder';\n\n&#x20;   container.appendChild(placeholderImg);\n\n&#x20;  &#x20;\n\n&#x20;   // 当图片进入视口时才加载真实图片\n\n&#x20;   const observer = new IntersectionObserver(entries => {\n\n&#x20;     entries.forEach(entry => {\n\n&#x20;       if (entry.isIntersecting && !this.realImage) {\n\n&#x20;         // 真正需要时才创建真实图片对象\n\n&#x20;         this.realImage = new RealImage(this.src);\n\n&#x20;         // 加载完成后替换占位图\n\n&#x20;         this.realImage.loadImage().then(() => {\n\n&#x20;           container.removeChild(placeholderImg);\n\n&#x20;           this.realImage.display(container);\n\n&#x20;         });\n\n&#x20;         observer.disconnect();\n\n&#x20;       }\n\n&#x20;     });\n\n&#x20;   });\n\n&#x20;  &#x20;\n\n&#x20;   observer.observe(placeholderImg);\n\n&#x20; }\n\n}\n\n// 使用\n\nconst container = document.getElementById('image-container');\n\n// 使用代理加载图片，而非直接使用RealImage\n\nconst image = new ImageProxy('https://example.com/large-image.jpg');\n\nimage.display(container);\n```\n\n#### 2. 保护代理（Protection Proxy）\n\n控制对目标对象的访问权限，常用于权限验证。\n\n**接口请求权限控制**：\n\n\n\n```\n// 真实接口服务\n\nclass ApiService {\n\n&#x20; getUserInfo(userId) {\n\n&#x20;   return \\`用户\\${userId}的信息\\`;\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; deleteUser(userId) {\n\n&#x20;   return \\`用户\\${userId}已删除\\`;\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; getDashboardData() {\n\n&#x20;   return \"仪表盘数据\";\n\n&#x20; }\n\n}\n\n// 权限代理\n\nclass ProtectedApiProxy {\n\n&#x20; constructor(apiService, userRole) {\n\n&#x20;   this.apiService = apiService;\n\n&#x20;   this.userRole = userRole; // 用户角色：admin/user/guest\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; // 公共接口，所有角色可访问\n\n&#x20; getUserInfo(userId) {\n\n&#x20;   return this.apiService.getUserInfo(userId);\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; // 受保护接口，只有管理员可访问\n\n&#x20; deleteUser(userId) {\n\n&#x20;   // 权限检查\n\n&#x20;   if (this.userRole !== 'admin') {\n\n&#x20;     throw new Error(\"权限不足：只有管理员可删除用户\");\n\n&#x20;   }\n\n&#x20;   // 权限通过，调用真实服务\n\n&#x20;   return this.apiService.deleteUser(userId);\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; // 受保护接口，管理员和普通用户可访问\n\n&#x20; getDashboardData() {\n\n&#x20;   if (\\['admin', 'user'].includes(this.userRole)) {\n\n&#x20;     return this.apiService.getDashboardData();\n\n&#x20;   }\n\n&#x20;   throw new Error(\"权限不足：请先登录\");\n\n&#x20; }\n\n}\n\n// 使用示例\n\nconst realApi = new ApiService();\n\n// 普通用户访问\n\nconst userProxy = new ProtectedApiProxy(realApi, 'user');\n\nconsole.log(userProxy.getUserInfo(123)); // 正常访问\n\nconsole.log(userProxy.getDashboardData()); // 正常访问\n\ntry {\n\n&#x20; userProxy.deleteUser(123); // 权限不足\n\n} catch (e) {\n\n&#x20; console.log(e.message);\n\n}\n\n// 管理员访问\n\nconst adminProxy = new ProtectedApiProxy(realApi, 'admin');\n\nconsole.log(adminProxy.deleteUser(123)); // 正常访问\n```\n\n#### 3. 缓存代理（Cache Proxy）\n\n为目标对象的计算结果提供缓存，避免重复计算。\n\n**计算结果缓存**：\n\n\n\n```\n// 复杂计算服务\n\nclass CalculationService {\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 计算斐波那契数列（耗时操作）\n\n&#x20;  \\* @param {number} n - 数列索引\n\n&#x20;  \\* @returns {number} 第n项的值\n\n&#x20;  \\*/\n\n&#x20; fibonacci(n) {\n\n&#x20;   console.log(\\`计算fibonacci(\\${n})\\`);\n\n&#x20;   if (n <= 1) return n;\n\n&#x20;   // 递归计算，效率低，适合缓存\n\n&#x20;   return this.fibonacci(n - 1) + this.fibonacci(n - 2);\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 计算两数乘积\n\n&#x20;  \\* @param {number} a - 第一个数\n\n&#x20;  \\* @param {number} b - 第二个数\n\n&#x20;  \\* @returns {number} 乘积\n\n&#x20;  \\*/\n\n&#x20; multiply(a, b) {\n\n&#x20;   console.log(\\`计算\\${a} \\* \\${b}\\`);\n\n&#x20;   return a \\* b;\n\n&#x20; }\n\n}\n\n// 缓存代理\n\nclass CacheProxy {\n\n&#x20; constructor(service) {\n\n&#x20;   this.service = service;\n\n&#x20;   this.cache = new Map(); // 缓存容器\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 带缓存的斐波那契计算\n\n&#x20;  \\* @param {number} n - 数列索引\n\n&#x20;  \\* @returns {number} 计算结果\n\n&#x20;  \\*/\n\n&#x20; fibonacci(n) {\n\n&#x20;   // 生成缓存键\n\n&#x20;   const key = \\`fib\\_\\${n}\\`;\n\n&#x20;  &#x20;\n\n&#x20;   // 检查缓存\n\n&#x20;   if (this.cache.has(key)) {\n\n&#x20;     console.log(\\`从缓存获取fibonacci(\\${n})\\`);\n\n&#x20;     return this.cache.get(key);\n\n&#x20;   }\n\n&#x20;  &#x20;\n\n&#x20;   // 无缓存，调用真实服务计算\n\n&#x20;   const result = this.service.fibonacci(n);\n\n&#x20;  &#x20;\n\n&#x20;   // 存入缓存\n\n&#x20;   this.cache.set(key, result);\n\n&#x20;  &#x20;\n\n&#x20;   return result;\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 带缓存的乘法计算\n\n&#x20;  \\* @param {number} a - 第一个数\n\n&#x20;  \\* @param {number} b - 第二个数\n\n&#x20;  \\* @returns {number} 乘积\n\n&#x20;  \\*/\n\n&#x20; multiply(a, b) {\n\n&#x20;   const key = \\`mul\\_\\${a}\\_\\${b}\\`;\n\n&#x20;   if (this.cache.has(key)) {\n\n&#x20;     console.log(\\`从缓存获取\\${a} \\* \\${b}\\`);\n\n&#x20;     return this.cache.get(key);\n\n&#x20;   }\n\n&#x20;  &#x20;\n\n&#x20;   const result = this.service.multiply(a, b);\n\n&#x20;   this.cache.set(key, result);\n\n&#x20;   return result;\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; /\\*\\*\n\n&#x20;  \\* 清除缓存\n\n&#x20;  \\*/\n\n&#x20; clearCache() {\n\n&#x20;   this.cache.clear();\n\n&#x20;   console.log(\"缓存已清除\");\n\n&#x20; }\n\n}\n\n// 使用示例\n\nconst calcService = new CalculationService();\n\nconst cacheProxy = new CacheProxy(calcService);\n\n// 第一次计算，无缓存\n\nconsole.log(cacheProxy.fibonacci(10)); // 会实际计算\n\n// 第二次计算，使用缓存\n\nconsole.log(cacheProxy.fibonacci(10)); // 从缓存获取\n\n// 乘法计算缓存\n\nconsole.log(cacheProxy.multiply(5, 8)); // 实际计算\n\nconsole.log(cacheProxy.multiply(5, 8)); // 从缓存获取\n```\n\n#### 4. 日志代理（Logging Proxy）\n\n记录目标对象的访问日志，用于调试或审计。\n\n\n\n```\n// 数据服务\n\nclass DataService {\n\n&#x20; fetchData(id) {\n\n&#x20;   return { id, name: \\`数据\\${id}\\` };\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; saveData(data) {\n\n&#x20;   return { success: true, id: data.id };\n\n&#x20; }\n\n}\n\n// 日志代理\n\nclass LoggingProxy {\n\n&#x20; constructor(service) {\n\n&#x20;   this.service = service;\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; fetchData(id) {\n\n&#x20;   // 记录访问日志\n\n&#x20;   console.log(\\`\\[\\${new Date().toISOString()}] 调用fetchData，参数：\\${id}\\`);\n\n&#x20;  &#x20;\n\n&#x20;   // 记录开始时间\n\n&#x20;   const start = Date.now();\n\n&#x20;  &#x20;\n\n&#x20;   // 调用真实服务\n\n&#x20;   const result = this.service.fetchData(id);\n\n&#x20;  &#x20;\n\n&#x20;   // 记录结束时间和结果\n\n&#x20;   const end = Date.now();\n\n&#x20;   console.log(\\`\\[\\${new Date().toISOString()}] fetchData返回：\\`, result);\n\n&#x20;   console.log(\\`\\[\\${new Date().toISOString()}] fetchData执行耗时：\\${end - start}ms\\`);\n\n&#x20;  &#x20;\n\n&#x20;   return result;\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; saveData(data) {\n\n&#x20;   console.log(\\`\\[\\${new Date().toISOString()}] 调用saveData，参数：\\`, data);\n\n&#x20;   const start = Date.now();\n\n&#x20;  &#x20;\n\n&#x20;   const result = this.service.saveData(data);\n\n&#x20;  &#x20;\n\n&#x20;   const end = Date.now();\n\n&#x20;   console.log(\\`\\[\\${new Date().toISOString()}] saveData返回：\\`, result);\n\n&#x20;   console.log(\\`\\[\\${new Date().toISOString()}] saveData执行耗时：\\${end - start}ms\\`);\n\n&#x20;  &#x20;\n\n&#x20;   return result;\n\n&#x20; }\n\n}\n\n// 使用\n\nconst dataService = new DataService();\n\nconst loggingProxy = new LoggingProxy(dataService);\n\nloggingProxy.fetchData(1);\n\nloggingProxy.saveData({ id: 2, name: \"新数据\" });\n```\n\n### 代理模式的优缺点分析\n\n**优点**：\n\n\n\n*   符合单一职责原则：目标对象专注于业务逻辑，代理专注于附加功能\n\n*   符合开闭原则：可在不修改目标对象和客户端的情况下添加新功能\n\n*   提高系统灵活性：可动态切换代理类型或增减代理功能\n\n*   保护目标对象：通过代理控制访问，隐藏目标对象细节\n\n**缺点**：\n\n\n\n*   增加系统复杂度：引入代理层可能使代码结构更复杂\n\n*   增加访问延迟：代理的额外处理会带来一定性能开销\n\n*   可能导致调试困难：多层代理可能使问题定位变得",
  "files": {
    "audio_answer_simple": "q5b43bef3_audio_answer_simple.mp3",
    "audio_answer_detail": "q5b43bef3_audio_answer_detail.mp3",
    "audio_answer_analysis": "q5b43bef3_audio_answer_analysis.mp3",
    "audio_question": "q5b43bef3_audio_question.mp3"
  }
}