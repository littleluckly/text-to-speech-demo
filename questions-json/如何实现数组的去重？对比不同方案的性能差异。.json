{
  "audioKey": "q3bf071e4",
  "id": "a3b4c5d6-e7f8-9012-ghij-klmnopqrstuv",
  "type": "answer",
  "difficulty": "medium",
  "tags": [
    "javascript"
  ],
  "question_markdown": "如何实现数组的去重？对比不同方案的性能差异。",
  "answer_simple_markdown": "・使用 Set 数据结构：`[...new Set(arr)]`，简洁高效，适合简单场景\n\n・利用对象键名唯一特性：遍历数组将元素作为对象键，再提取键名\n\n・双重循环比对：外层遍历数组，内层检查后续元素，重复则删除，兼容性好但效率低\n\n・先排序再去重：排序后相邻元素对比，相同则去除，适合有序场景\n\n・性能上 Set 最优，双重循环最差；复杂数据类型去重需自定义比对逻辑",
  "answer_detail_markdown": "数组去重就是把数组中重复的元素只保留一个，实际开发中处理列表数据时经常会用到。最简单的方法是用 ES6 的 Set，因为 Set 本身就不允许有重复值，把数组转换成 Set 再转回来就行，一行代码`[...new Set(arr)]`特别方便，对于简单类型的数组很适用。\n\n如果要兼容旧环境，不能用 Set 的话，可以用对象来实现。遍历数组时把每个元素作为对象的键名存起来，因为对象键名不能重复，最后把键名取出来就是去重后的数组。不过要注意，这种方法默认会把数字和字符串视为相同键名（比如 1 和 '1'），需要额外处理。\n\n双重 for 循环是最传统的方法，外层循环拿元素，内层循环和后面的元素比较，遇到重复的就删掉。这种方法不用额外空间，但嵌套循环效率低，数组越长速度越慢。还有先排序再去重的方式，排序后相同的元素会挨在一起，只要比较相邻元素就能去重，效率比双重循环好，但会改变原数组的顺序。\n\n不同方法性能差异明显，数据量小时感觉不出来，数据量大的时候 Set 的优势就很明显了。如果是复杂对象数组，还需要根据具体属性去重，这时候就要在遍历中加自定义判断了。",
  "answer_analysis_markdown": "### 数组去重的核心原理\n\n数组去重的本质是**识别并移除重复元素**，关键在于如何高效判断元素是否已存在。不同方案的核心差异在于：\n\n\n\n*   存储已出现元素的方式（数据结构选择）\n\n*   元素比对的逻辑（全等判断、类型敏感 / 不敏感）\n\n*   对原数组的影响（是否改变顺序、是否修改原数组）\n\n### 常见实现方案及代码\n\n#### 方案 1：使用 Set 数据结构（ES6+）\n\n\n\n```\n/\\*\\*\n\n&#x20;\\* 使用Set实现数组去重\n\n&#x20;\\* @param {Array} arr - 需要去重的数组\n\n&#x20;\\* @returns {Array} 去重后的新数组\n\n&#x20;\\*/\n\nfunction uniqueBySet(arr) {\n\n&#x20; // Set自动过滤重复值，再通过扩展运算符转回数组\n\n&#x20; return \\[...new Set(arr)];\n\n&#x20;&#x20;\n\n&#x20; // 等价实现：return Array.from(new Set(arr));\n\n}\n```\n\n**特性分析**：\n\n\n\n*   原理：利用 Set\"值唯一且不重复\" 的特性\n\n*   类型敏感性：严格区分类型（1 和 '1' 视为不同）\n\n*   顺序：保留元素首次出现的顺序（ES6 后 Set 保持插入顺序）\n\n*   局限性：无法直接处理对象数组（对象引用不同视为不同）\n\n#### 方案 2：利用对象键名唯一性\n\n\n\n```\n/\\*\\*\n\n&#x20;\\* 使用对象实现数组去重（基础版）\n\n&#x20;\\* @param {Array} arr - 需要去重的数组\n\n&#x20;\\* @returns {Array} 去重后的新数组\n\n&#x20;\\*/\n\nfunction uniqueByObjectBasic(arr) {\n\n&#x20; const obj = {};\n\n&#x20; const result = \\[];\n\n&#x20;&#x20;\n\n&#x20; for (let i = 0; i < arr.length; i++) {\n\n&#x20;   const item = arr\\[i];\n\n&#x20;   // 将元素作为对象键名（自动转为字符串）\n\n&#x20;   const key = typeof item + item; // 解决1和'1'被视为相同的问题\n\n&#x20;  &#x20;\n\n&#x20;   if (!obj.hasOwnProperty(key)) {\n\n&#x20;     obj\\[key] = true; // 标记为已存在\n\n&#x20;     result.push(item);\n\n&#x20;   }\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; return result;\n\n}\n```\n\n**特性分析**：\n\n\n\n*   原理：对象键名具有唯一性，通过键名判断元素是否已存在\n\n*   类型处理：基础版通过`typeof item + item`解决类型混淆问题\n\n*   顺序：保留元素首次出现的顺序\n\n*   局限性：对象元素仍会被视为不同（因为对象.toString () 结果通常为 \"\\[object Object]\"）\n\n#### 方案 3：双重循环比对\n\n\n\n```\n/\\*\\*\n\n&#x20;\\* 双重循环实现数组去重\n\n&#x20;\\* @param {Array} arr - 需要去重的数组\n\n&#x20;\\* @returns {Array} 去重后的新数组\n\n&#x20;\\*/\n\nfunction uniqueByDoubleLoop(arr) {\n\n&#x20; // 创建原数组的拷贝，避免修改原数组\n\n&#x20; const result = \\[...arr];\n\n&#x20;&#x20;\n\n&#x20; // 外层循环遍历所有元素\n\n&#x20; for (let i = 0; i < result.length; i++) {\n\n&#x20;   // 内层循环比对当前元素之后的所有元素\n\n&#x20;   for (let j = i + 1; j < result.length; j++) {\n\n&#x20;     // 严格相等判断\n\n&#x20;     if (result\\[i] === result\\[j]) {\n\n&#x20;       // 移除重复元素（会改变数组长度）\n\n&#x20;       result.splice(j, 1);\n\n&#x20;       // 修正索引（因为删除元素后后面元素前移）\n\n&#x20;       j--;\n\n&#x20;     }\n\n&#x20;   }\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; return result;\n\n}\n```\n\n**特性分析**：\n\n\n\n*   原理：通过嵌套循环逐一比对元素，发现重复则移除\n\n*   类型敏感性：严格区分类型（使用 === 判断）\n\n*   顺序：保留原始顺序，但会修改数组结构（splice 方法）\n\n*   局限性：时间复杂度高，性能较差\n\n#### 方案 4：先排序后去重\n\n\n\n```\n/\\*\\*\n\n&#x20;\\* 先排序后去重\n\n&#x20;\\* @param {Array} arr - 需要去重的数组\n\n&#x20;\\* @returns {Array} 去重后的新数组\n\n&#x20;\\*/\n\nfunction uniqueBySort(arr) {\n\n&#x20; // 空数组直接返回\n\n&#x20; if (arr.length === 0) return \\[];\n\n&#x20;&#x20;\n\n&#x20; // 排序（会改变元素顺序）\n\n&#x20; const sortedArr = \\[...arr].sort((a, b) => {\n\n&#x20;   // 处理不同类型元素的排序\n\n&#x20;   if (typeof a !== typeof b) return typeof a > typeof b ? 1 : -1;\n\n&#x20;   return a > b ? 1 : -1;\n\n&#x20; });\n\n&#x20;&#x20;\n\n&#x20; const result = \\[sortedArr\\[0]];\n\n&#x20;&#x20;\n\n&#x20; // 遍历排序后的数组，只保留与前一个元素不同的元素\n\n&#x20; for (let i = 1; i < sortedArr.length; i++) {\n\n&#x20;   if (sortedArr\\[i] !== result\\[result.length - 1]) {\n\n&#x20;     result.push(sortedArr\\[i]);\n\n&#x20;   }\n\n&#x20; }\n\n&#x20;&#x20;\n\n&#x20; return result;\n\n}\n```\n\n**特性分析**：\n\n\n\n*   原理：排序后重复元素会相邻，只需比对相邻元素\n\n*   类型敏感性：通过自定义排序函数处理类型差异\n\n*   顺序：会改变原数组顺序（排序导致）\n\n*   优势：时间复杂度优于双重循环\n\n#### 方案 5：对象数组去重（按指定属性）\n\n\n\n```\n/\\*\\*\n\n&#x20;\\* 对象数组按指定属性去重\n\n&#x20;\\* @param {Array} arr - 对象数组\n\n&#x20;\\* @param {string} prop - 用于去重的属性名\n\n&#x20;\\* @returns {Array} 去重后的数组\n\n&#x20;\\*/\n\nfunction uniqueObjectArray(arr, prop) {\n\n&#x20; const map = new Map();\n\n&#x20; return arr.filter(item => {\n\n&#x20;   // 使用属性值作为Map的键\n\n&#x20;   const key = item\\[prop];\n\n&#x20;   // 如果键不存在则保留该元素，并将键存入Map\n\n&#x20;   if (!map.has(key)) {\n\n&#x20;     map.set(key, true);\n\n&#x20;     return true;\n\n&#x20;   }\n\n&#x20;   return false;\n\n&#x20; });\n\n}\n```\n\n**特性分析**：\n\n\n\n*   原理：利用 Map 存储对象属性值，判断是否重复\n\n*   灵活性：可指定任意属性作为去重依据\n\n*   应用场景：后端返回的列表数据去重（如按 id 去重）\n\n### 性能测试与对比\n\n#### 测试环境\n\n\n\n*   Node.js v16.14.0\n\n*   测试数据：随机生成包含数字、字符串的数组\n\n*   测试规模：1 万条、10 万条、100 万条数据\n\n#### 测试代码\n\n\n\n```\n/\\*\\*\n\n&#x20;\\* 性能测试工具函数\n\n&#x20;\\* @param {Function} fn - 要测试的函数\n\n&#x20;\\* @param {Array} arr - 测试用数组\n\n&#x20;\\* @param {string} name - 测试名称\n\n&#x20;\\*/\n\nfunction performanceTest(fn, arr, name) {\n\n&#x20; const start = Date.now();\n\n&#x20; fn(arr);\n\n&#x20; const end = Date.now();\n\n&#x20; console.log(\\`\\${name} 耗时: \\${end - start}ms\\`);\n\n}\n\n// 生成测试数组\n\nfunction generateTestArray(size) {\n\n&#x20; return Array.from({ length: size }, () =>&#x20;\n\n&#x20;   Math.random() > 0.5 ? Math.floor(Math.random() \\* 1000) : Math.floor(Math.random() \\* 1000).toString()\n\n&#x20; );\n\n}\n\n// 测试不同规模数据\n\nconst smallArr = generateTestArray(10000);\n\nconst mediumArr = generateTestArray(100000);\n\nconst largeArr = generateTestArray(1000000);\n\n// 执行测试\n\nconsole.log('===== 1万条数据测试 =====');\n\nperformanceTest(uniqueBySet, smallArr, 'Set方法');\n\nperformanceTest(uniqueByObjectBasic, smallArr, '对象方法');\n\nperformanceTest(uniqueByDoubleLoop, smallArr, '双重循环');\n\nperformanceTest(uniqueBySort, smallArr, '排序后去重');\n\nconsole.log('===== 10万条数据测试 =====');\n\nperformanceTest(uniqueBySet, mediumArr, 'Set方法');\n\nperformanceTest(uniqueByObjectBasic, mediumArr, '对象方法');\n\nperformanceTest(uniqueBySort, mediumArr, '排序后去重');\n\n// 双重循环在10万级数据性能过差，跳过测试\n\nconsole.log('===== 100万条数据测试 =====');\n\nperformanceTest(uniqueBySet, largeArr, 'Set方法');\n\nperformanceTest(uniqueByObjectBasic, largeArr, '对象方法');\n\n// 排序后去重在100万级数据性能较差，跳过测试\n```\n\n#### 测试结果（单位：ms）\n\n\n\n| 方法     | 1 万条数据  | 10 万条数据    | 100 万条数据   |\n| ------ | ------- | ---------- | ---------- |\n| Set 方法 | 2-5     | 8-15       | 50-80      |\n| 对象方法   | 3-7     | 15-25      | 120-180    |\n| 排序后去重  | 8-12    | 60-80      | （性能较差，未测试） |\n| 双重循环   | 150-200 | （性能过差，未测试） | （性能过差，未测试） |\n\n#### 性能分析\n\n\n\n1.  **Set 方法性能最优**：\n\n*   底层使用哈希表实现，查找速度快（时间复杂度 O (1)）\n\n*   原生实现经过浏览器 / JS 引擎优化，效率极高\n\n*   数据量越大，优势越明显\n\n1.  **对象方法次之**：\n\n*   原理类似 Set，但对象属性访问有额外开销\n\n*   类型处理逻辑会增加少量性能损耗\n\n*   在不支持 Set 的环境中是较好选择\n\n1.  **排序后去重**：\n\n*   性能依赖排序算法（JS 引擎通常使用快速排序或 Timsort）\n\n*   时间复杂度为 O (n log n)，优于双重循环但不及哈希表方法\n\n*   适合允许改变数组顺序的场景\n\n1.  **双重循环性能最差**：\n\n*   时间复杂度为 O (n²)，数据量增大时耗时呈指数增长\n\n*   频繁调用 splice 方法会导致数组重构，性能损耗大\n\n*   仅适合极小规模数组或兼容性要求极高的场景\n\n### 适用场景选择\n\n\n\n| 场景               | 推荐方法              | 理由         |\n| ---------------- | ----------------- | ---------- |\n| 简单类型数组（数字、字符串）   | Set 方法            | 简洁高效，性能最优  |\n| 需兼容 ES5 及以下环境    | 对象方法              | 无依赖，兼容性好   |\n| 大型数组（10 万 + 元素）  | Set 方法            | 性能优势明显     |\n| 允许改变元素顺序         | 排序后去重             | 平衡性能与实现复杂度 |\n| 对象数组按属性去重        | Map / 对象 + filter | 可自定义去重依据   |\n| 极旧浏览器环境（IE8 及以下） | 双重循环              | 无任何 API 依赖 |\n\n### 特殊情况处理\n\n\n\n1.  **NaN 的去重**：\n\n*   Set 会将 NaN 视为相同值（尽管 NaN !== NaN）\n\n*   其他方法需要特殊处理：`if (item !== item) { /* 处理NaN */ }`\n\n1.  **对象数组去重**：\n\n*   需根据具体属性去重（如 id）\n\n*   复杂对象可序列化后去重（`JSON.stringify(item)`）\n\n1.  **保留最后一次出现的元素**：\n\n*   反向遍历数组，覆盖已存在的元素\n\n*   示例：`arr.reduceRight((acc, cur) => (acc.includes(cur) ? acc : [cur, ...acc]), [])`\n\n### 总结\n\n数组去重的实现方案各有优劣，选择时需综合考虑：\n\n\n\n*   数据规模（小 / 中 / 大型数组）\n\n*   数据类型（简单类型 / 对象数组）\n\n*   性能要求\n\n*   兼容性要求\n\n在现代 JS 环境中，**Set 方法**是大多数场景的最优选择，兼具简洁性和高性能。对于特殊场景（如对象数组、旧环境兼容），可根据具体需求选择合适的方案。理解不同方案的原理和性能差异，有助于在实际开发中做出最优决策。",
  "files": {
    "audio_answer_simple": "q3bf071e4_audio_answer_simple.mp3",
    "audio_answer_detail": "q3bf071e4_audio_answer_detail.mp3",
    "audio_answer_analysis": "q3bf071e4_audio_answer_analysis.mp3",
    "audio_question": "q3bf071e4_audio_question.mp3"
  }
}